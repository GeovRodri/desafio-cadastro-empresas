{
  "name": "desafio-cadastro-empresas",
  "version": "0.0.0",
  "scripts": {
    "deploy": "cd front-end/ && npm run build --prod && cd .. && firebase deploy"
  },
  "private": true,
  "dependencies": {
    "firebase-admin": "^10.0.0"
  }
}
